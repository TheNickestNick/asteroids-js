define(function() {
  // TODO: implement freelists for these things

  var Spanwer = {
    spawnBullet: function(x, y, vx, vy) {
      return { x: x, y: y, vx: vx, vy: vy };
    }
  };

  return Spawner;
});
